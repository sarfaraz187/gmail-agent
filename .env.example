# OpenAI API Key (required)
OPENAI_API_KEY=sk-your-api-key-here

# Application settings
DEBUG=false

# LLM Settings (optional - these have defaults)
# Options: gpt-4o-mini (default, cheap), gpt-4o (best quality, 10x more expensive)
OPENAI_MODEL=gpt-4o-mini
MAX_TOKENS=500
TEMPERATURE=0.7
